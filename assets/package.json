{
  "name": "myflames-assets",
  "version": "0.1.0",
  "private": true,
  "description": "Build-time workspace that bundles the lesson runtime. Emits myflames/assets/anim-runtime.js which Python reads at render time. Run 'npm install && npm run build' to regenerate the committed bundle.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "d3-hierarchy": "^3.1.2",
    "d3-shape": "^3.2.0",
    "motion": "^11.11.17"
  },
  "devDependencies": {
    "@types/d3-hierarchy": "^3.1.7",
    "@types/d3-shape": "^3.1.6",
    "esbuild": "^0.24.0",
    "puppeteer": "^24.42.0",
    "typescript": "^5.6.3"
  }
}
